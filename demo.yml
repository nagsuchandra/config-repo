prop:
  key: value11

registrationId: 500

LOG_LEVEL_PATTERN: TEST

logging:
  pattern:
    level: ${LOG_LEVEL_PATTERN:-%5p} userId:%X{userId} tenantId:%X{tenantId} tenantGuid:%X{tenantGuid} registrationId:%X{registrationId}
